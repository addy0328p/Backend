// Import the add function from the math module
const math = require("./math");
//Use cntrl +space to see built in packages  example https,fs etc.

// Call the add function with arguments 5 and 3, and log the result
console.log(math.subFn(5, 3));