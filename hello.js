console.log('Hello world');
// All the DOM manipulation is removed like window objects in node.Only core functiaonlitie are added to perfom core.

//npm init create file to run the project.

// npm init creates package.json files in the   project directory.
// This is the package.json file for a Node.js project named "backend".
// It contains metadata about the project, including its name, version, scripts, author, license, and description.
// The "scripts" section defines two scripts: "start" and "test".
// The "start" script runs "node hello.js", which means it will start the application defined in the "hello.js" file.
// The "test" script currently just prints an error message and exits with a status code of 1, indicating that no tests are specified.